using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TempScriptTest : MonoBehaviour
{
    public GameObject BasePoint;
    private BoxcastTest test;

    // Start is called before the first frame update
    void Start()
    {
        //print(Vector3.Distance(this.transform.position, BasePoint.transform.position));
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
